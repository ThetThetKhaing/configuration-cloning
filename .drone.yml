kind: pipeline
type: docker
name: default

clone:
  disable: true

steps:
- name: fetch
  image: alpine/git
  commands:
  - git fetch --tags

- name: clone
  image: alpine/git
  commands:
  - git clone https://github.com/octocat/hello-world.git .
  - git checkout test
